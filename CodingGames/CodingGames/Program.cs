using System;
using System.Linq;

namespace CodingGames
{
    class Program
    {
        static void Main(string[] args)
        {
            int M = 10;
            Enumerable.Range(0, M).ToList().ForEach(delegate(int M)
            { 
                
            }
            );
        }
    }
}
